package com.crm.crm_backend.model.enums;

public enum PricingRuleType {
    /** Legacy / seed-data alias used in shared Neon DB rows. */
    DISCOUNT,
    PERCENTAGE_DISCOUNT,
    FIXED_DISCOUNT,
    PERCENTAGE_MARKUP
}
